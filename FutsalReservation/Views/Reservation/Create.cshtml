@model FutsalReservation.Data.Entities.Reservation

@{
    ViewData["Title"] = "Create";
    ViewData["UserId"] = ViewBag.UserId;
    //string Tomorrow = $"{date.Year}/{date.Month}/{date.Day+1}";
    //string[] Timings = new string[] { "5am - 6am", "6am - 7am", "7am - 8am", "8am - 9am", "9am - 10am", "10am - 11am", "11am - 12pm", "12pm - 1pm", "1pm - 2pm", "2pm - 3pm", "3pm - 4pm", "4pm - 5pm", "5pm - 6pm", "6pm - 7pm", "7pm - 8pm", "8pm - 9pm", "9pm - 10pm" };
    //var timing = new SelectList(Timings);
}

<h1>Create</h1>

<h4>Reservation</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="UserId" class="control-label"></label>
                <select asp-for="UserId" class="form-control" asp-items="ViewBag.UserId"></select>
            </div>
            <div class="form-group">
                <label asp-for="CourtId" class="control-label"></label>
                <select asp-for="CourtId" class ="form-control" asp-items="ViewBag.CourtId"></select>
            </div>
            <div class="form-group">
                <label asp-for="ReservationDate" class="control-label"></label>
                <select asp-for="ReservationDate" class="form-control" >
                    <option value="@ViewBag.Today">Today</option>
                </select>
                <span asp-validation-for="ReservationDate" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="ReservationTime" class="control-label"></label>
                <select asp-for="ReservationTime" class ="form-control" asp-items="ViewBag.Timings"></select>
                <span asp-validation-for="ReservationTime" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

@if(User.IsInRole("Admin")){
<div>
    <a asp-action="Index">Back to List</a>
</div>
}

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
